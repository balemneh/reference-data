<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="012-001-add-partial-status-to-batch" author="reference-data">
        <comment>Add PARTIAL status to chk_batch_status constraint in bulk_import_batches table</comment>

        <sql>
            ALTER TABLE reference_data.bulk_import_batches
            DROP CONSTRAINT chk_batch_status;
        </sql>

        <sql>
            ALTER TABLE reference_data.bulk_import_batches
            ADD CONSTRAINT chk_batch_status
            CHECK (status IN ('PENDING', 'VALIDATING', 'PROCESSING', 'COMPLETED', 'FAILED', 'CANCELLED', 'PARTIAL'));
        </sql>

        <rollback>
            <sql>
                ALTER TABLE reference_data.bulk_import_batches
                DROP CONSTRAINT chk_batch_status;
            </sql>
            <sql>
                ALTER TABLE reference_data.bulk_import_batches
                ADD CONSTRAINT chk_batch_status
                CHECK (status IN ('PENDING', 'VALIDATING', 'PROCESSING', 'COMPLETED', 'FAILED', 'CANCELLED'));
            </sql>
        </rollback>
    </changeSet>

</databaseChangeLog>